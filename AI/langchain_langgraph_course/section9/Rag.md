# Rag

- Get the doc and split to smaller chunks. (preprocessing step needed)
- take the user query and find the most relevant chunk


Langchain documentloader

text splitter -> split a text into chunks

embeddings -> it creates vectors from text -> easy to find close neighbors (similar vectors) -> based on this we can find relevant chunks
